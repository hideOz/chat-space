.main__messages__message{data: {message: {id: message.id}}}
  .main__messages__message__upper
    .main__messages__message__upper__talker
      = message.user.name
    .main__messages__message__upper__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .main__messages__message__lower
    - if message.content.present?
      %p.main__messages__message__lower__text
        = message.content
    = image_tag message.image.url if message.image.present?